/** Shared page footer. */
export function SiteFooter() {
  return (
    <footer className="border-t border-zinc-900">
      <div className="mx-auto max-w-5xl px-6 py-6 text-xs text-zinc-500">
        AnchorNet — liquidity coordination for Stellar anchors.
      </div>
    </footer>
  );
}
